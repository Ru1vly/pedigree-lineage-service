{{- if .Values.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pedigree-lineage.fullname" . }}-api
  labels:
    {{- include "pedigree-lineage.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pedigree-lineage.apiSelectorLabels" . | nindent 4 }}
{{- end }}
